# Implementation Plan

- [x] 1. 動画ID一覧取得のマルチタブ対応
- [x] 1.1 内部ヘルパーの導入とマルチタブフェッチの実装
  - 既存の `list_channel_video_ids` 内にある単一タブからのID抽出ロジックを、独立した内部ヘルパーメソッドとして切り出す
  - チャンネルURLから末尾のタブパス（`/videos`, `/streams` 等）を除去してベースURLを正規化し、`/videos` と `/streams` をそれぞれ付加して2回フェッチする
  - 両タブの結果を出現順に結合し、重複するIDは最初の出現のみ保持して排除する
  - タブごとのフェッチを独立した例外ハンドリングで囲み、片方が失敗してももう片方の結果で処理を継続する
  - 両方失敗した場合は空リストを返す
  - タブごとの検出件数とマージ後の総件数をINFOレベルでログ出力する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.2, 3.4_

- [x] 2. テスト
- [x] 2.1 マルチタブ取得のユニットテスト
  - 両タブから正常に動画IDが取得されマージ・重複排除される場合のテスト
  - `/videos` のみ失敗し `/streams` の結果のみで返却される場合のテスト
  - `/streams` のみ失敗し `/videos` の結果のみで返却される場合のテスト
  - 両タブとも失敗した場合に空リストが返る場合のテスト
  - 両タブに同一動画IDが含まれる場合の重複排除テスト
  - タブごとの件数がINFOレベルでログ出力される検証
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.2, 3.4_

- [x] 2.2 (P) 既存テストの回帰確認
  - 既存のSyncServiceテスト（差分同期、スキップ処理、unavailable除外、live_statusフィルタ）がすべてパスすることを確認する
  - CLIの同期完了サマリー表示が従来通り動作することを確認する
  - _Requirements: 2.1, 3.1, 3.2, 3.3_
